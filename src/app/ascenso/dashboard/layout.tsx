import ProgramSwitcherLink from '@/app/dashboard/ProgramSwitcherLink'

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <><ProgramSwitcherLink />{children}</>
}
